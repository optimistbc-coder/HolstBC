import {Box} from "@mantine/core";

export default function OrderFormTitle() {

    return (
        <Box className={"orderFormTitleContainer"}>
            <p>ЗАПОВНІТЬ ФОРМУ</p>
        </Box>
    );
}